<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    android:focusable="false"
    android:focusableInTouchMode="false"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ImageView
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/item_correo_imagen"/>

    <TextView
        android:padding="15dp"
        android:id="@+id/item_correo_titulo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignBottom="@+id/item_correo_imagen"
        android:layout_toRightOf="@+id/item_correo_imagen"
        android:layout_toEndOf="@+id/item_correo_imagen" />

</RelativeLayout>